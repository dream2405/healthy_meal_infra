apiVersion: v1
kind: Service
metadata:
  name: {{ include "healthy-meal-backend.fullname" . }}
  labels:
    {{- include "healthy-meal-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "healthy-meal-backend.selectorLabels" . | nindent 4 }}